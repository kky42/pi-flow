# Real-model E2E credentials. Claude Code E2Es route through DeepSeek's
# Anthropic-compatible endpoint and never fall back to Anthropic login.
DEEPSEEK_API_KEY=

# DEEPSEEK_API_TOKEN is also accepted. Use --deepseek-api-key-env <name>
# when the credential lives in a different environment variable.
